# Server-only database connection. Never expose this as a Vite/Vercel client
# variable (for example, do not prefix it with VITE_ or NEXT_PUBLIC_).
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require